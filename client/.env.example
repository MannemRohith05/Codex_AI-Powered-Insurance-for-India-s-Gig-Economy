# Frontend .env for Vercel deployment
# Leave empty for local dev (uses Vite proxy to localhost:5000)
# Set to your Render backend URL for production:
VITE_API_URL=https://your-gigshield-api.onrender.com
