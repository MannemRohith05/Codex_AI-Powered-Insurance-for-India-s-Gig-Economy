version: "3"
services:
  app:
    build: .
    ports:
      - "5173:5173"
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
      - /app/client/node_modules
      - /app/server/node_modules
    command: npm start
